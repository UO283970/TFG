package com.example.tfg.model

class AppConstants{
    companion object {
        const val PASS_MIN_CHARACTERS: Int = 8
        const val DESC_MAX_CHARACTERS: Int = 150
        const val LIST_DESC_MAX_CHARACTERS: Int = 150
    }
}